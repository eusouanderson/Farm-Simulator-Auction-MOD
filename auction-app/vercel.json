{
  "version": 2,
  "builds": [
    {
      "src": "backend/conect.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/conect.js" },
    { "src": "/(.*)", "dest": "/build/$1" }
  ]
}
